# Runtime dependencies for the src/ pipeline on HPC.
# If your cluster provides its own CUDA-matched PyTorch wheels/modules,
# replace the torch/torchvision lines below with the site-specific build.

torch>=2.1
torchvision>=0.16
PyYAML>=6.0
datasets>=4.0
torchcodec>=0.1
av>=13.0
Pillow>=10.0
wandb>=0.18
